generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum UserRole {
  SUPER_ADMIN
  STAFF
  REGISTERED_USER
  PROVIDER
}

enum UserStatus {
  PENDING
  APPROVED
  REJECTED
  SUSPENDED
  BLOCKED
}

enum AccountType {
  REGISTERED_USER
  PROVIDER
  ADMIN
}

enum GiftStatus {
  ACTIVE
  INACTIVE
  OUT_OF_STOCK
}

enum BroadcastStatus {
  DRAFT
  SCHEDULED
  PROCESSING
  SENT
  CANCELLED
  FAILED
}

enum BroadcastAudience {
  ALL_USERS
  PROVIDER
  USER
}

enum NotificationRecipientType {
  ADMIN
  PROVIDER
  REGISTERED_USER
}

enum DevicePlatform {
  ANDROID
  IOS
  WEB
}

enum AuditLogStatus {
  SUCCESS
  FAILED
  PENDING
  WARNING
}

enum AuditLogSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum SubscriptionPlanStatus {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum SubscriptionPlanVisibility {
  PUBLIC
  PRIVATE
  ARCHIVED
}

enum BillingCycle {
  MONTHLY
  YEARLY
}

enum PromotionalOfferDiscountType {
  PERCENTAGE
  FIXED_AMOUNT
}

enum PromotionalOfferStatus {
  ACTIVE
  SCHEDULED
  EXPIRED
  INACTIVE
  PENDING
  REJECTED
}

enum PromotionalOfferApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PromotionalOfferRejectionReason {
  INVALID_DISCOUNT
  INVALID_DATES
  POLICY_VIOLATION
  ITEM_NOT_ELIGIBLE
  INCOMPLETE_INFORMATION
  OTHER
}

enum UploadedFileStatus {
  PENDING
  COMPLETED
  FAILED
  DELETED
}

enum CustomerReminderEventType {
  BIRTHDAY
  ANNIVERSARY
  HOLIDAY
  CUSTOM
}

enum CustomerEventType {
  BIRTHDAY
  ANNIVERSARY
  HOLIDAY
  WORK_MILESTONE
  CUSTOM
}

enum CustomerEventReminderTiming {
  ONE_DAY_BEFORE
  THREE_DAYS_BEFORE
  ONE_WEEK_BEFORE
  ON_THE_DAY
  CUSTOM
}

enum CustomerEventReminderFrequency {
  ONE_TIME
  MONTHLY
  YEARLY
}

enum CustomerEventReminderChannel {
  PUSH
  EMAIL
  SMS
}

enum CustomerEventReminderJobStatus {
  PENDING
  SENT
  FAILED
  CANCELLED
}

enum OrderStatus {
  PENDING
  ACCEPTED
  PROCESSING
  SHIPPED
  DELIVERED
  COMPLETED
  CANCELLED
  REJECTED
}

enum PaymentStatus {
  PENDING
  PROCESSING
  SUCCEEDED
  FAILED
  CANCELLED
  REFUNDED
}

enum RefundRequestStatus {
  REQUESTED
  APPROVED
  REJECTED
  REFUND_PROCESSING
  REFUNDED
  FAILED
}

enum RefundRejectReason {
  ITEM_DELIVERED_AS_DESCRIBED
  NO_DAMAGE_EVIDENCE
  REFUND_WINDOW_EXPIRED
  NOT_COVERED_BY_POLICY
  OTHER
}

enum PaymentMethod {
  STRIPE_CARD
  E_WALLET
  BANK_TRANSFER
  COD
  PLACEHOLDER
}

enum PaymentProvider {
  STRIPE
  MANUAL
}

enum MoneyGiftStatus {
  DRAFT
  PAYMENT_PENDING
  PAID
  SCHEDULED
  SENT
  FAILED
  CANCELLED
}

enum CustomerRecurringPaymentFrequency {
  DAILY
  WEEKLY
  MONTHLY
  YEARLY
}

enum CustomerRecurringPaymentStatus {
  ACTIVE
  PAUSED
  CANCELLED
  EXPIRED
  FAILED
}

enum CustomerRecurringPaymentOccurrenceStatus {
  PENDING
  SUCCESS
  FAILED
  SKIPPED
  CANCELLED
}

enum CustomerRecurringPaymentCancelMode {
  IMMEDIATELY
  AFTER_CURRENT_BILLING_CYCLE
}

enum ReferralStatus {
  PENDING
  JOINED
  QUALIFIED
  REWARDED
  EXPIRED
}

enum RewardLedgerType {
  EARNED
  REDEEMED
  EXPIRED
  ADJUSTED
}

enum RewardLedgerSource {
  REFERRAL
  ADMIN_ADJUSTMENT
  PROMOTION
}

enum ReferralExpirationUnit {
  DAYS
  WEEKS
  MONTHS
}

enum ReferralQualificationRule {
  FIRST_SUCCESSFUL_PURCHASE
}

enum WalletOwnerType {
  USER
  PROVIDER
  PLATFORM
}

enum WalletLedgerType {
  TOP_UP
  ORDER_PAYMENT
  MONEY_GIFT_SENT
  ORDER_EARNING
  PLATFORM_FEE
  REFUND
  REWARD_CREDIT
  WITHDRAWAL
  ADJUSTMENT
  PENALTY
}

enum WalletLedgerDirection {
  CREDIT
  DEBIT
}

enum WalletLedgerStatus {
  PENDING
  SUCCESS
  FAILED
  CANCELLED
}

enum CustomerSubscriptionStatus {
  INCOMPLETE
  ACTIVE
  TRIALING
  PAST_DUE
  CANCELLED
  EXPIRED
}

enum CustomerSubscriptionInvoiceStatus {
  PAID
  OPEN
  FAILED
  VOID
}

enum CustomerSubscriptionCancelMode {
  IMMEDIATELY
  AFTER_CURRENT_PERIOD
}

enum ProviderFulfillmentMethod {
  PICKUP
  DELIVERY
}

enum SocialPostVisibility {
  PUBLIC
  HIDDEN
}

enum SocialPostStatus {
  ACTIVE
  REMOVED
}

enum SocialReportReason {
  SPAM
  HARASSMENT
  HATE_SPEECH
  FALSE_INFORMATION
  INAPPROPRIATE
  INAPPROPRIATE_BEHAVIOR
  INAPPROPRIATE_CONTENT
  DECEPTIVE_LINK
  POLICY_VIOLATION
  OTHER
}

enum SocialReportSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum SocialReportStatus {
  PENDING
  REVIEWED
  HIDDEN
  REMOVED
  WARNED
  DISMISSED
}

enum SocialModerationAction {
  HIDE
  REMOVE
  WARN_USER
  MARK_REVIEWED
  DISMISS_REPORT
  RESTORE
}

enum SocialReportingEscalationRule {
  AUTO_HIDE_CONTENT
  SEND_TO_SENIOR_MODERATOR
  IMMEDIATE_REVIEW
  FACT_CHECK_QUEUE
  MANUAL_REVIEW
}

enum ReviewStatus {
  PUBLISHED
  PENDING
  FLAGGED
  HIDDEN
  REMOVED
  PENALIZED
}

enum ReviewSource {
  GOOGLE
  TRUSTPILOT
  APP_STORE
  IN_APP
}

enum ReviewSeverity {
  CRITICAL
  HIGH
  MEDIUM
  LOW
}

enum ReviewFlagReason {
  SPAM
  ABUSE
  FAKE_REVIEW
  POLICY_VIOLATION
  FALSE_POSITIVE
  OFF_TOPIC
  OTHER
}

enum ReviewModerationActorType {
  ADMIN
  SUPER_ADMIN
  SYSTEM
}

enum ReviewModerationAction {
  APPROVE
  HIDE
  REMOVE
  PENALIZE
  RESTORE
  MARK_SPAM
  MARK_FAKE
  AUTO_HIDDEN
  AUTO_APPROVED
  AUTO_FLAGGED
}

enum SenderType {
  USER
  PROVIDER
}

enum ProviderReportReason {
  FRAUDULENT_ACTIVITY
  INAPPROPRIATE_CONTENT
  FAKE_REVIEWS
  POOR_SERVICE_QUALITY
  NOT_RESPONSIVE
  OTHER
}

enum ProviderReportStatus {
  SUBMITTED
  UNDER_REVIEW
  RESOLVED
  REJECTED
}

enum UserSafetyReportReason {
  HARASSMENT
  SPAM
  INAPPROPRIATE_CONTENT
  FRAUD_OR_SCAM
  OFF_PLATFORM_PAYMENT
  IMPERSONATION
  OTHER
}

enum UserSafetySourceType {
  PROFILE
  CHAT
  FEED_ITEM
  GROUP_GIFT
  OTHER
}

enum UserSafetyReportStatus {
  SUBMITTED
  UNDER_REVIEW
  REVIEWED
  WARNED
  SUSPENDED
  DISMISSED
  ESCALATED
}

enum UserSafetyAdminAction {
  MARK_REVIEWED
  WARN_REPORTED_USER
  SUSPEND_REPORTED_USER
  DISMISS_REPORT
  ESCALATE
}

enum DisputeStatus {
  PENDING
  UNDER_REVIEW
  APPROVED
  REJECTED
}

enum ProviderDisputeDecision {
  APPROVED
  REJECTED
}

enum DisputeDecisionReason {
  INSUFFICIENT_EVIDENCE
  INVALID_CLAIM
  OUTSIDE_POLICY
  DUPLICATE_DISPUTE
  PROVIDER_PROOF_VALID
  OTHER
}

enum ProviderPayoutMethodType {
  BANK_ACCOUNT
}

enum ProviderPayoutAccountType {
  CHECKING
  SAVINGS
  CURRENT
  OTHER
}

enum ProviderPayoutExternalProvider {
  STRIPE_CONNECT
  PLAID
  MANUAL
}

enum ProviderPayoutVerificationStatus {
  PENDING
  VERIFIED
  FAILED
}

enum ProviderPayoutStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
  ON_HOLD
  REJECTED
}

model User {
  id                                  String                               @id @default(cuid())
  email                               String                               @unique
  password                            String
  role                                UserRole                             @default(REGISTERED_USER)
  status                              UserStatus                           @default(PENDING)
  firstName                           String                               @map("first_name")
  lastName                            String                               @map("last_name")
  phone                               String?
  avatarUrl                           String?                              @map("avatar_url")
  mustChangePassword                  Boolean                              @default(false) @map("must_change_password")
  lastLoginAt                         DateTime?                            @map("last_login_at") @db.Timestamptz
  verificationOtp                     String?                              @map("verification_otp")
  verificationOtpExpiresAt            DateTime?                            @map("verification_otp_expires_at") @db.Timestamptz
  verificationOtpAttempts             Int                                  @default(0) @map("verification_otp_attempts")
  resetPasswordOtp                    String?                              @map("reset_password_otp")
  resetPasswordOtpExpiresAt           DateTime?                            @map("reset_password_otp_expires_at") @db.Timestamptz
  resetPasswordOtpAttempts            Int                                  @default(0) @map("reset_password_otp_attempts")
  suspensionReason                    String?                              @map("suspension_reason")
  suspensionComment                   String?                              @map("suspension_comment")
  referralCode                        String?                              @unique @map("referral_code")
  createdAt                           DateTime                             @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                           DateTime                             @updatedAt @map("updated_at") @db.Timestamptz
  staffProfile                        StaffProfile?
  providerProfile                     ProviderProfile?
  customerProfile                     CustomerProfile?
  auditLogs                           AdminAuditLog[]                      @relation("AuditActor")
  providerGifts                       Gift[]                               @relation("GiftProvider")
  promotionalOffers                   PromotionalOffer[]                   @relation("PromotionalOfferProvider")
  approvedPromotionalOffers           PromotionalOffer[]                   @relation("PromotionalOfferApprover")
  rejectedPromotionalOffers           PromotionalOffer[]                   @relation("PromotionalOfferRejecter")
  createdBroadcasts                   Broadcast[]                          @relation("BroadcastCreator")
  updatedBroadcasts                   Broadcast[]                          @relation("BroadcastUpdater")
  notifications                       Notification[]
  deviceTokens                        DeviceToken[]
  authSessions                        AuthSession[]
  payments                            Payment[]
  moneyGifts                          MoneyGift[]
  customerRecurringPayments           CustomerRecurringPayment[]
  customerRecurringPaymentOccurrences CustomerRecurringPaymentOccurrence[]
  customerPaymentMethods              CustomerPaymentMethod[]
  wallets                             Wallet[]
  customerBankAccounts                CustomerBankAccount[]
  customerSubscriptions               CustomerSubscription[]
  customerSubscriptionInvoices        CustomerSubscriptionInvoice[]
  sentReferrals                       Referral[]                           @relation("ReferralReferrer")
  receivedReferrals                   Referral[]                           @relation("ReferralReferred")
  rewardLedger                        RewardLedger[]
  uploadedFiles                       UploadedFile[]
  updatedReferralSettings             ReferralSettings[]                   @relation("ReferralSettingsUpdater")
  cart                                Cart?
  customerOrders                      Order[]                              @relation("CustomerOrders")
  providerOrders                      Order[]                              @relation("ProviderOrders")
  customerRefundRequests              RefundRequest[]                      @relation("RefundRequestCustomer")
  providerRefundRequests              RefundRequest[]                      @relation("RefundRequestProvider")
  customerReviews                     Review[]                             @relation("ReviewCustomer")
  providerReviews                     Review[]                             @relation("ReviewProvider")
  reviewResponses                     ReviewResponse[]                     @relation("ReviewResponseProvider")
  updatedReviewPolicies               ReviewPolicy[]                       @relation("ReviewPolicyUpdater")
  userConversations                   Conversation[]                       @relation("UserConversation")
  providerConversations               Conversation[]                       @relation("ProviderConversation")
  submittedProviderReports            ProviderReport[]                     @relation("ProviderReportReporter")
  receivedProviderReports             ProviderReport[]                     @relation("ProviderReportProvider")
  reviewedProviderReports             ProviderReport[]                     @relation("ProviderReportReviewer")
  disputesAsUser                      Dispute[]                            @relation("DisputeUser")
  disputesAsProvider                  Dispute[]                            @relation("DisputeProvider")
  providerPayoutMethods               ProviderPayoutMethod[]
  updatedSystemSettings               SystemSettings[]                     @relation("SystemSettingsUpdater")
  updatedCommissionTiers              CommissionTier[]                     @relation("CommissionTierUpdater")
  socialPosts                         SocialPost[]                         @relation("SocialPostAuthor")
  socialReports                       SocialReport[]                       @relation("SocialReportReporter")
  submittedUserSafetyReports          UserSafetyReport[]                   @relation("UserSafetyReporter")
  receivedUserSafetyReports           UserSafetyReport[]                   @relation("UserSafetyReported")
  reviewedUserSafetyReports           UserSafetyReport[]                   @relation("UserSafetyReviewer")
  blockedUsers                        UserBlock[]                          @relation("UserBlockBlocker")
  blockedByUsers                      UserBlock[]                          @relation("UserBlockBlocked")
  socialWarnings                      UserWarning[]                        @relation("WarnedUser")
  issuedSocialWarnings                UserWarning[]                        @relation("WarningIssuer")

  @@index([role, status])
  @@index([referralCode])
  @@map("users")
}

model StaffProfile {
  id          String  @id @default(cuid())
  userId      String  @unique @map("user_id")
  staffRoleId String? @map("staff_role_id")

  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  staffRole StaffRole? @relation(fields: [staffRoleId], references: [id])

  @@index([staffRoleId])
  @@map("staff_profiles")
}

model ProviderProfile {
  id                 String   @id @default(cuid())
  userId             String   @unique @map("user_id")
  businessName       String?  @map("business_name")
  legalName          String?  @map("legal_name")
  businessCategoryId String?  @map("business_category_id")
  taxId              String?  @map("tax_id")
  businessEmail      String?  @map("business_email")
  businessPhone      String?  @map("business_phone")
  businessAddress    String?  @map("business_address")
  fulfillmentMethods Json?    @map("fulfillment_methods")
  companyLogoUrl     String   @default("") @map("company_logo_url")
  coverImageUrl      String   @default("") @map("cover_image_url")
  businessBio        String?  @map("business_bio")
  lat                Float?
  lng                Float?
  createdAt          DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt          DateTime @updatedAt @map("updated_at") @db.Timestamptz

  user              User                      @relation(fields: [userId], references: [id], onDelete: Cascade)
  businessCategory  ProviderBusinessCategory? @relation(fields: [businessCategoryId], references: [id])
  providerDocuments ProviderDocument[]

  @@index([businessCategoryId])
  @@map("provider_profiles")
}

model CustomerProfile {
  id                      String    @id @default(cuid())
  userId                  String    @unique @map("user_id")
  dateOfBirth             DateTime? @map("date_of_birth") @db.Date
  gender                  String?
  preferredCurrency       String    @default("USD") @map("preferred_currency")
  preferredLanguage       String    @default("en") @map("preferred_language")
  marketingOptIn          Boolean   @default(false) @map("marketing_opt_in")
  giftPreferencesJson     Json      @default("{}") @map("gift_preferences_json")
  occasionPreferencesJson Json      @default("{}") @map("occasion_preferences_json")
  createdAt               DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt               DateTime  @updatedAt @map("updated_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("customer_profiles")
}

model Referral {
  id                               String         @id @default(cuid())
  referrerUserId                   String         @map("referrer_user_id")
  referredUserId                   String         @unique @map("referred_user_id")
  referralCode                     String         @map("referral_code")
  status                           ReferralStatus @default(JOINED)
  rewardAmount                     Decimal        @default(0) @map("reward_amount") @db.Decimal(10, 2)
  currency                         String         @default("USD")
  referrerRewardAmountSnapshot     Decimal        @default(0) @map("referrer_reward_amount_snapshot") @db.Decimal(10, 2)
  newUserRewardAmountSnapshot      Decimal        @default(0) @map("new_user_reward_amount_snapshot") @db.Decimal(10, 2)
  rewardCurrencySnapshot           String         @default("USD") @map("reward_currency_snapshot")
  minimumTransactionAmountSnapshot Decimal        @default(0) @map("minimum_transaction_amount_snapshot") @db.Decimal(10, 2)
  expiresAt                        DateTime?      @map("expires_at") @db.Timestamptz
  joinedAt                         DateTime?      @map("joined_at") @db.Timestamptz
  qualifiedAt                      DateTime?      @map("qualified_at") @db.Timestamptz
  rewardedAt                       DateTime?      @map("rewarded_at") @db.Timestamptz
  createdAt                        DateTime       @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                        DateTime       @updatedAt @map("updated_at") @db.Timestamptz

  referrer User @relation("ReferralReferrer", fields: [referrerUserId], references: [id])
  referred User @relation("ReferralReferred", fields: [referredUserId], references: [id])

  @@index([referrerUserId, status])
  @@index([referralCode])
  @@map("referrals")
}

model ReferralSettings {
  id                       String                    @id @default(cuid())
  isActive                 Boolean                   @default(true) @map("is_active")
  referrerRewardAmount     Decimal                   @default(10) @map("referrer_reward_amount") @db.Decimal(10, 2)
  newUserRewardAmount      Decimal                   @default(0) @map("new_user_reward_amount") @db.Decimal(10, 2)
  rewardCurrency           String                    @default("USD") @map("reward_currency")
  minimumTransactionAmount Decimal                   @default(0) @map("minimum_transaction_amount") @db.Decimal(10, 2)
  referralExpirationValue  Int                       @default(30) @map("referral_expiration_value")
  referralExpirationUnit   ReferralExpirationUnit    @default(DAYS) @map("referral_expiration_unit")
  allowSelfReferrals       Boolean                   @default(false) @map("allow_self_referrals")
  qualificationRule        ReferralQualificationRule @default(FIRST_SUCCESSFUL_PURCHASE) @map("qualification_rule")
  updatedById              String?                   @map("updated_by_id")
  createdAt                DateTime                  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                DateTime                  @updatedAt @map("updated_at") @db.Timestamptz

  updatedBy User? @relation("ReferralSettingsUpdater", fields: [updatedById], references: [id])

  @@index([updatedById])
  @@map("referral_settings")
}

model RefundPolicySettings {
  id                           String   @id @default(cuid())
  allowCancellation            Boolean  @default(true) @map("allow_cancellation")
  cancellationDeductionPercent Decimal  @default(0) @map("cancellation_deduction_percent") @db.Decimal(5, 2)
  currency                     String   @default("USD")
  createdAt                    DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                    DateTime @updatedAt @map("updated_at") @db.Timestamptz

  @@map("refund_policy_settings")
}

model RewardLedger {
  id          String             @id @default(cuid())
  userId      String             @map("user_id")
  type        RewardLedgerType
  amount      Decimal            @db.Decimal(10, 2)
  currency    String             @default("USD")
  source      RewardLedgerSource
  sourceId    String?            @map("source_id")
  description String
  createdAt   DateTime           @default(now()) @map("created_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id])

  @@unique([userId, type, source, sourceId])
  @@index([userId, type, createdAt])
  @@index([source, sourceId])
  @@map("reward_ledgers")
}

model Wallet {
  id          String          @id @default(cuid())
  ownerType   WalletOwnerType @map("owner_type")
  ownerId     String?         @map("owner_id") // null = PLATFORM singleton
  balance     Decimal         @default(0) @db.Decimal(10, 2) // real, withdrawable money
  giftCredits Decimal         @default(0) @map("gift_credits") @db.Decimal(10, 2) // USER-only, non-withdrawable
  currency    String          @default("USD")
  createdAt   DateTime        @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime        @updatedAt @map("updated_at") @db.Timestamptz

  owner User? @relation(fields: [ownerId], references: [id])

  ledger      WalletLedger[]
  withdrawals WalletWithdrawal[]

  @@unique([ownerType, ownerId])
  @@map("wallets")
}

model WalletLedger {
  id             String                @id @default(cuid())
  walletId       String                @map("wallet_id")
  type           WalletLedgerType
  direction      WalletLedgerDirection
  amount         Decimal               @db.Decimal(10, 2)
  currency       String                @default("USD")
  status         WalletLedgerStatus    @default(PENDING)
  orderId        String?               @map("order_id")
  paymentId      String?               @map("payment_id") // set only when a Stripe/gateway Payment is involved
  withdrawalId   String?               @map("withdrawal_id") // set only when type = WITHDRAWAL
  rewardLedgerId String?               @map("reward_ledger_id")
  transactionId  String                @unique @map("transaction_id")
  description    String
  createdAt      DateTime              @default(now()) @map("created_at") @db.Timestamptz

  wallet     Wallet            @relation(fields: [walletId], references: [id])
  order      Order?            @relation(fields: [orderId], references: [id])
  payment    Payment?          @relation(fields: [paymentId], references: [id])
  withdrawal WalletWithdrawal? @relation(fields: [withdrawalId], references: [id])

  @@unique([walletId, orderId, type]) // idempotency: an order can only credit/debit a given wallet once per ledger type
  @@index([walletId, createdAt])
  @@index([orderId])
  @@index([paymentId])
  @@index([rewardLedgerId])
  @@map("wallet_ledgers")
}

model WalletWithdrawal {
  id                String               @id @default(cuid())
  walletId          String               @map("wallet_id")
  bankAccountId     String               @map("bank_account_id") // CustomerBankAccount.id or ProviderPayoutMethod.id
  amount            Decimal              @db.Decimal(10, 2)
  processingFee     Decimal              @default(0) @map("processing_fee") @db.Decimal(10, 2)
  totalToReceive    Decimal              @map("total_to_receive") @db.Decimal(10, 2)
  currency          String               @default("USD")
  status            ProviderPayoutStatus @default(PENDING)
  externalPayoutId  String?              @map("external_payout_id")
  failureReason     String?              @map("failure_reason")
  expectedArrivalAt DateTime?            @map("expected_arrival_at") @db.Timestamptz
  completedAt       DateTime?            @map("completed_at") @db.Timestamptz
  transactionId     String               @unique @map("transaction_id")
  idempotencyKey    String?              @unique @map("idempotency_key")
  createdAt         DateTime             @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime             @updatedAt @map("updated_at") @db.Timestamptz

  wallet Wallet         @relation(fields: [walletId], references: [id])
  ledger WalletLedger[]

  @@index([walletId, status, createdAt])
  @@map("wallet_withdrawals")
}

model CustomerBankAccount {
  id                String    @id @default(cuid())
  userId            String    @map("user_id")
  accountHolderName String    @map("account_holder_name")
  bankName          String    @map("bank_name")
  maskedAccount     String    @map("masked_account")
  last4             String
  isDefault         Boolean   @default(false) @map("is_default")
  createdAt         DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime  @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt         DateTime? @map("deleted_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id])

  @@index([userId, deletedAt])
  @@index([userId, isDefault])
  @@map("customer_bank_accounts")
}

model ProviderBusinessCategory {
  id          String   @id @default(cuid())
  name        String   @unique
  slug        String   @unique
  description String?
  imageUrl    String?  @map("image_url")
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime @updatedAt @map("updated_at") @db.Timestamptz

  providers ProviderProfile[]

  @@index([isActive])
  @@map("provider_business_categories")
}

model GiftCategory {
  id          String   @id @default(cuid())
  name        String
  slug        String   @unique
  description String?
  imageUrl    String?  @map("image_url")
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime @updatedAt @map("updated_at") @db.Timestamptz

  gifts Gift[]

  @@index([isActive])
  @@map("gift_categories")
}

model CustomerWishlist {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  giftId    String   @map("gift_id")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz

  @@unique([userId, giftId])
  @@index([userId])
  @@index([giftId])
  @@map("customer_wishlists")
}

model CustomerAddress {
  id                   String    @id @default(cuid())
  userId               String    @map("user_id")
  label                String
  fullName             String    @map("full_name")
  phone                String
  line1                String
  line2                String?
  city                 String
  state                String?
  country              String
  postalCode           String?   @map("postal_code")
  latitude             Decimal?  @db.Decimal(10, 7)
  longitude            Decimal?  @db.Decimal(10, 7)
  deliveryInstructions String?   @map("delivery_instructions")
  isDefault            Boolean   @default(false) @map("is_default")
  createdAt            DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt            DateTime  @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt            DateTime? @map("deleted_at") @db.Timestamptz

  @@index([userId, deletedAt])
  @@index([userId, isDefault])
  @@map("customer_addresses")
}

model CustomerReminder {
  id            String                    @id @default(cuid())
  userId        String                    @map("user_id")
  title         String
  recipientName String                    @map("recipient_name")
  eventType     CustomerReminderEventType @map("event_type")
  reminderDate  DateTime                  @map("reminder_date") @db.Timestamptz
  notes         String?
  isActive      Boolean                   @default(true) @map("is_active")
  createdAt     DateTime                  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime                  @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt     DateTime?                 @map("deleted_at") @db.Timestamptz

  @@index([userId, deletedAt])
  @@index([reminderDate, isActive])
  @@map("customer_reminders")
}

model CustomerContact {
  id                String                     @id @default(cuid())
  userId            String                     @map("user_id")
  name              String
  relationship      String?
  phone             String?
  email             String?
  address           String?
  likes             String?
  avatarUrl         String?                    @map("avatar_url")
  birthday          DateTime?                  @db.Date
  notes             String?
  createdAt         DateTime                   @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime                   @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt         DateTime?                  @map("deleted_at") @db.Timestamptz
  events            CustomerEvent[]
  moneyGifts        MoneyGift[]
  recurringPayments CustomerRecurringPayment[]

  @@index([userId])
  @@index([userId, name])
  @@index([userId, relationship])
  @@index([userId, deletedAt])
  @@map("customer_contacts")
}

model CustomerEvent {
  id                 String                         @id @default(cuid())
  userId             String                         @map("user_id")
  recipientId        String?                        @map("recipient_id")
  eventType          CustomerEventType              @map("event_type")
  title              String
  eventDate          DateTime                       @map("event_date") @db.Timestamptz
  reminderTiming     CustomerEventReminderTiming    @default(ON_THE_DAY) @map("reminder_timing")
  reminderFrequency  CustomerEventReminderFrequency @default(ONE_TIME) @map("reminder_frequency")
  customAlertTime    String?                        @map("custom_alert_time")
  channelsJson       Json                           @default("[\"PUSH\"]") @map("channels_json")
  notes              String?
  isActive           Boolean                        @default(true) @map("is_active")
  lastReminderSentAt DateTime?                      @map("last_reminder_sent_at") @db.Timestamptz
  nextReminderAt     DateTime?                      @map("next_reminder_at") @db.Timestamptz
  createdAt          DateTime                       @default(now()) @map("created_at") @db.Timestamptz
  updatedAt          DateTime                       @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt          DateTime?                      @map("deleted_at") @db.Timestamptz
  recipient          CustomerContact?               @relation(fields: [recipientId], references: [id])
  reminderJobs       CustomerEventReminderJob[]

  @@index([userId, deletedAt])
  @@index([userId, eventDate])
  @@index([userId, eventType])
  @@index([recipientId])
  @@map("customer_events")
}

model CustomerEventReminderJob {
  id            String                         @id @default(cuid())
  eventId       String                         @map("event_id")
  userId        String                         @map("user_id")
  scheduledFor  DateTime                       @map("scheduled_for") @db.Timestamptz
  channel       CustomerEventReminderChannel
  status        CustomerEventReminderJobStatus @default(PENDING)
  failureReason String?                        @map("failure_reason")
  sentAt        DateTime?                      @map("sent_at") @db.Timestamptz
  createdAt     DateTime                       @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime                       @updatedAt @map("updated_at") @db.Timestamptz
  event         CustomerEvent                  @relation(fields: [eventId], references: [id])

  @@index([eventId, status])
  @@index([userId, scheduledFor])
  @@map("customer_event_reminder_jobs")
}

model Cart {
  id        String   @id @default(cuid())
  userId    String   @unique @map("user_id")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz

  // Relations
  user  User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  items CartItem[]

  @@map("carts")
}

model CartItem {
  id        String   @id @default(cuid())
  cartId    String   @map("cart_id")
  giftId    String   @map("gift_id")
  variantId String?  @map("variant_id")
  quantity  Int
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz

  // Relations
  cart    Cart         @relation(fields: [cartId], references: [id], onDelete: Cascade)
  gift    Gift         @relation(fields: [giftId], references: [id])
  variant GiftVariant? @relation(fields: [variantId], references: [id])

  @@index([cartId])
  @@map("cart_items")
}

model Payment {
  id                      String          @id @default(cuid())
  userId                  String          @map("user_id")
  orderId                 String?         @map("order_id")
  moneyGiftId             String?         @unique @map("money_gift_id")
  customerSubscriptionId  String?         @map("customer_subscription_id")
  provider                PaymentProvider
  providerPaymentIntentId String?         @unique @map("provider_payment_intent_id")
  amount                  Decimal         @db.Decimal(10, 2)
  currency                String
  status                  PaymentStatus   @default(PENDING)
  paymentMethod           PaymentMethod   @map("payment_method")
  failureReason           String?         @map("failure_reason")
  metadataJson            Json            @default("{}") @map("metadata_json")
  idempotencyKey          String?         @unique @map("idempotency_key")
  createdAt               DateTime        @default(now()) @map("created_at") @db.Timestamptz
  updatedAt               DateTime        @updatedAt @map("updated_at") @db.Timestamptz

  user                        User                                 @relation(fields: [userId], references: [id])
  order                       Order?                               @relation(fields: [orderId], references: [id])
  moneyGift                   MoneyGift?                           @relation(fields: [moneyGiftId], references: [id])
  customerSubscription        CustomerSubscription?                @relation(fields: [customerSubscriptionId], references: [id])
  recurringPaymentOccurrences CustomerRecurringPaymentOccurrence[]
  refundRequests              RefundRequest[]
  walletLedgerEntries         WalletLedger[]

  @@index([userId, status])
  @@index([status, createdAt])
  @@index([userId, idempotencyKey])
  @@map("payments")
}

model CustomerSubscription {
  id                   String                     @id @default(cuid())
  userId               String                     @map("user_id")
  planId               String                     @map("plan_id")
  billingCycle         BillingCycle               @map("billing_cycle")
  status               CustomerSubscriptionStatus @default(INCOMPLETE)
  isPremium            Boolean                    @default(false) @map("is_premium")
  stripeCustomerId     String?                    @map("stripe_customer_id")
  stripeSubscriptionId String?                    @unique @map("stripe_subscription_id")
  stripePriceId        String?                    @map("stripe_price_id")
  currentPeriodStart   DateTime?                  @map("current_period_start") @db.Timestamptz
  currentPeriodEnd     DateTime?                  @map("current_period_end") @db.Timestamptz
  cancelAtPeriodEnd    Boolean                    @default(false) @map("cancel_at_period_end")
  cancelledAt          DateTime?                  @map("cancelled_at") @db.Timestamptz
  createdAt            DateTime                   @default(now()) @map("created_at") @db.Timestamptz
  updatedAt            DateTime                   @updatedAt @map("updated_at") @db.Timestamptz

  user     User                          @relation(fields: [userId], references: [id])
  plan     SubscriptionPlan              @relation(fields: [planId], references: [id])
  invoices CustomerSubscriptionInvoice[]
  payments Payment[]

  @@index([userId, status])
  @@index([planId])
  @@map("customer_subscriptions")
}

model CustomerSubscriptionInvoice {
  id                     String                            @id @default(cuid())
  userId                 String                            @map("user_id")
  customerSubscriptionId String                            @map("customer_subscription_id")
  stripeInvoiceId        String                            @unique @map("stripe_invoice_id")
  stripePaymentIntentId  String?                           @map("stripe_payment_intent_id")
  amountDue              Decimal                           @map("amount_due") @db.Decimal(10, 2)
  amountPaid             Decimal                           @map("amount_paid") @db.Decimal(10, 2)
  currency               String
  status                 CustomerSubscriptionInvoiceStatus
  invoicePdfUrl          String?                           @map("invoice_pdf_url")
  hostedInvoiceUrl       String?                           @map("hosted_invoice_url")
  billingReason          String?                           @map("billing_reason")
  createdAt              DateTime                          @default(now()) @map("created_at") @db.Timestamptz

  user         User                 @relation(fields: [userId], references: [id])
  subscription CustomerSubscription @relation(fields: [customerSubscriptionId], references: [id])

  @@index([userId, createdAt])
  @@index([customerSubscriptionId])
  @@map("customer_subscription_invoices")
}

model Order {
  id              String      @id @default(cuid())
  orderNumber     String      @unique @map("order_number")
  userId          String      @map("user_id")
  providerId      String      @map("provider_id")
  status          OrderStatus @default(PENDING)
  rejectionReason String?     @map("rejection_reason")

  // Pricing
  subtotal    Decimal @db.Decimal(10, 2)
  platformFee Decimal @default(0.00) @map("platform_fee") @db.Decimal(10, 2)
  total       Decimal @db.Decimal(10, 2)

  // Checkout & Emotional Gifting Payload
  recipientName    String  @map("recipient_name")
  recipientPhone   String  @map("recipient_phone")
  recipientAddress String  @map("recipient_address")
  giftMessage      String? @map("gift_message")
  mediaAttachments Json?   @map("media_attachments")

  // Cancellation
  cancellationDeductionPercent Decimal?  @map("cancellation_deduction_percent") @db.Decimal(5, 2)
  cancelledAt                  DateTime? @map("cancelled_at") @db.Timestamptz
  cancellationRefundAmount     Decimal?  @map("cancellation_refund_amount") @db.Decimal(10, 2)

  // Group Gifting
  isGroupGift          Boolean  @default(false) @map("is_group_gift")
  groupTargetAmount    Decimal? @map("group_target_amount") @db.Decimal(10, 2)
  groupCollectedAmount Decimal  @default(0.00) @map("group_collected_amount") @db.Decimal(10, 2)

  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz

  // Relations
  user                User             @relation("CustomerOrders", fields: [userId], references: [id])
  provider            User             @relation("ProviderOrders", fields: [providerId], references: [id])
  items               OrderItem[]
  payments            Payment[]
  disputes            Dispute[]
  reviews             Review[]
  refundRequests      RefundRequest[]
  conversations       Conversation[]
  providerReports     ProviderReport[]
  walletLedgerEntries WalletLedger[]

  @@index([userId])
  @@index([providerId, status])
  @@map("orders")
}

model OrderItem {
  id          String  @id @default(cuid())
  orderId     String  @map("order_id")
  giftId      String  @map("gift_id")
  variantId   String? @map("variant_id")
  variantName String? @map("variant_name")
  quantity    Int
  unitPrice   Decimal @map("unit_price") @db.Decimal(10, 2)

  // Relations
  order   Order        @relation(fields: [orderId], references: [id], onDelete: Cascade)
  gift    Gift         @relation(fields: [giftId], references: [id])
  variant GiftVariant? @relation(fields: [variantId], references: [id])

  @@map("order_items")
}

model RefundRequest {
  id               String              @id @default(cuid())
  orderId          String              @map("order_id")
  userId           String              @map("user_id")
  providerId       String              @map("provider_id")
  paymentId        String?             @map("payment_id")
  requestedAmount  Decimal             @map("requested_amount") @db.Decimal(10, 2)
  approvedAmount   Decimal?            @map("approved_amount") @db.Decimal(10, 2)
  currency         String              @default("USD")
  customerReason   String              @map("customer_reason")
  evidenceUrlsJson Json                @default("[]") @map("evidence_urls_json")
  status           RefundRequestStatus @default(REQUESTED)
  rejectionReason  RefundRejectReason? @map("rejection_reason")
  providerComment  String?             @map("provider_comment")
  stripeRefundId   String?             @map("stripe_refund_id")
  transactionId    String?             @unique @map("transaction_id")
  requestedAt      DateTime            @default(now()) @map("requested_at") @db.Timestamptz
  approvedAt       DateTime?           @map("approved_at") @db.Timestamptz
  rejectedAt       DateTime?           @map("rejected_at") @db.Timestamptz
  refundedAt       DateTime?           @map("refunded_at") @db.Timestamptz
  createdAt        DateTime            @default(now()) @map("created_at") @db.Timestamptz
  updatedAt        DateTime            @updatedAt @map("updated_at") @db.Timestamptz

  order    Order    @relation(fields: [orderId], references: [id])
  user     User     @relation("RefundRequestCustomer", fields: [userId], references: [id])
  provider User     @relation("RefundRequestProvider", fields: [providerId], references: [id])
  payment  Payment? @relation(fields: [paymentId], references: [id])

  @@index([providerId, status, createdAt])
  @@index([orderId])
  @@index([userId])
  @@map("refund_requests")
}

model MoneyGift {
  id                   String          @id @default(cuid())
  userId               String          @map("user_id")
  recipientContactId   String          @map("recipient_contact_id")
  paymentId            String?         @unique @map("payment_id")
  amount               Decimal         @db.Decimal(10, 2)
  currency             String
  message              String?
  messageMediaUrlsJson Json            @default("[]") @map("message_media_urls_json")
  deliveryDate         DateTime        @map("delivery_date") @db.Timestamptz
  repeatAnnually       Boolean         @default(false) @map("repeat_annually")
  status               MoneyGiftStatus @default(DRAFT)
  createdAt            DateTime        @default(now()) @map("created_at") @db.Timestamptz
  updatedAt            DateTime        @updatedAt @map("updated_at") @db.Timestamptz

  user                        User                                 @relation(fields: [userId], references: [id])
  recipientContact            CustomerContact                      @relation(fields: [recipientContactId], references: [id])
  payment                     Payment?
  recurringPaymentOccurrences CustomerRecurringPaymentOccurrence[]

  @@index([userId, status, createdAt])
  @@index([recipientContactId])
  @@map("money_gifts")
}

model CustomerRecurringPayment {
  id                    String                            @id @default(cuid())
  userId                String                            @map("user_id")
  recipientContactId    String                            @map("recipient_contact_id")
  amount                Decimal                           @db.Decimal(10, 2)
  currency              String
  frequency             CustomerRecurringPaymentFrequency
  scheduleJson          Json                              @map("schedule_json")
  message               String?
  messageMediaUrlsJson  Json                              @default("[]") @map("message_media_urls_json")
  paymentMethod         PaymentMethod                     @map("payment_method")
  stripePaymentMethodId String?                           @map("stripe_payment_method_id")
  status                CustomerRecurringPaymentStatus    @default(ACTIVE)
  nextBillingAt         DateTime                          @map("next_billing_at") @db.Timestamptz
  startDate             DateTime                          @map("start_date") @db.Timestamptz
  endDate               DateTime?                         @map("end_date") @db.Timestamptz
  cancelAt              DateTime?                         @map("cancel_at") @db.Timestamptz
  cancelAtPeriodEnd     Boolean                           @default(false) @map("cancel_at_period_end")
  cancelledAt           DateTime?                         @map("cancelled_at") @db.Timestamptz
  cancelReason          String?                           @map("cancel_reason")
  failureCount          Int                               @default(0) @map("failure_count")
  deletedAt             DateTime?                         @map("deleted_at") @db.Timestamptz
  createdAt             DateTime                          @default(now()) @map("created_at") @db.Timestamptz
  updatedAt             DateTime                          @updatedAt @map("updated_at") @db.Timestamptz

  user             User                                 @relation(fields: [userId], references: [id])
  recipientContact CustomerContact                      @relation(fields: [recipientContactId], references: [id])
  occurrences      CustomerRecurringPaymentOccurrence[]

  @@index([userId, status, nextBillingAt])
  @@index([recipientContactId])
  @@index([status, nextBillingAt])
  @@index([deletedAt])
  @@map("customer_recurring_payments")
}

model CustomerRecurringPaymentOccurrence {
  id                 String                                   @id @default(cuid())
  recurringPaymentId String                                   @map("recurring_payment_id")
  userId             String                                   @map("user_id")
  paymentId          String?                                  @map("payment_id")
  moneyGiftId        String?                                  @map("money_gift_id")
  amount             Decimal                                  @db.Decimal(10, 2)
  currency           String
  scheduledFor       DateTime                                 @map("scheduled_for") @db.Timestamptz
  processedAt        DateTime?                                @map("processed_at") @db.Timestamptz
  status             CustomerRecurringPaymentOccurrenceStatus @default(PENDING)
  failureReason      String?                                  @map("failure_reason")
  createdAt          DateTime                                 @default(now()) @map("created_at") @db.Timestamptz
  updatedAt          DateTime                                 @updatedAt @map("updated_at") @db.Timestamptz

  recurringPayment CustomerRecurringPayment @relation(fields: [recurringPaymentId], references: [id])
  user             User                     @relation(fields: [userId], references: [id])
  payment          Payment?                 @relation(fields: [paymentId], references: [id])
  moneyGift        MoneyGift?               @relation(fields: [moneyGiftId], references: [id])

  @@index([recurringPaymentId, status])
  @@index([userId, scheduledFor])
  @@index([status, scheduledFor])
  @@map("customer_recurring_payment_occurrences")
}

model CustomerPaymentMethod {
  id                    String    @id @default(cuid())
  userId                String    @map("user_id")
  stripeCustomerId      String    @map("stripe_customer_id")
  stripePaymentMethodId String    @unique @map("stripe_payment_method_id")
  type                  String
  brand                 String?
  last4                 String?
  expiryMonth           Int?      @map("expiry_month")
  expiryYear            Int?      @map("expiry_year")
  isDefault             Boolean   @default(false) @map("is_default")
  createdAt             DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt             DateTime  @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt             DateTime? @map("deleted_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id])

  @@index([userId, deletedAt])
  @@index([userId, isDefault])
  @@map("customer_payment_methods")
}

model PromotionalOffer {
  id               String                           @id @default(cuid())
  providerId       String                           @map("provider_id")
  itemId           String                           @map("item_id")
  title            String
  description      String?
  discountType     PromotionalOfferDiscountType     @map("discount_type")
  discountValue    Decimal                          @map("discount_value") @db.Decimal(10, 2)
  startDate        DateTime                         @map("start_date") @db.Timestamptz
  endDate          DateTime?                        @map("end_date") @db.Timestamptz
  eligibilityRules String?                          @map("eligibility_rules")
  isActive         Boolean                          @default(true) @map("is_active")
  status           PromotionalOfferStatus           @default(PENDING)
  approvalStatus   PromotionalOfferApprovalStatus   @default(PENDING) @map("approval_status")
  approvedAt       DateTime?                        @map("approved_at") @db.Timestamptz
  approvedBy       String?                          @map("approved_by")
  rejectedAt       DateTime?                        @map("rejected_at") @db.Timestamptz
  rejectedBy       String?                          @map("rejected_by")
  rejectionReason  PromotionalOfferRejectionReason? @map("rejection_reason")
  rejectionComment String?                          @map("rejection_comment")
  createdBy        String?                          @map("created_by")
  updatedBy        String?                          @map("updated_by")
  deletedAt        DateTime?                        @map("deleted_at") @db.Timestamptz
  createdAt        DateTime                         @default(now()) @map("created_at") @db.Timestamptz
  updatedAt        DateTime                         @updatedAt @map("updated_at") @db.Timestamptz

  provider User  @relation("PromotionalOfferProvider", fields: [providerId], references: [id])
  item     Gift  @relation(fields: [itemId], references: [id])
  approver User? @relation("PromotionalOfferApprover", fields: [approvedBy], references: [id])
  rejecter User? @relation("PromotionalOfferRejecter", fields: [rejectedBy], references: [id])

  @@index([providerId, status])
  @@index([itemId, status])
  @@index([approvalStatus, status])
  @@index([startDate, endDate])
  @@index([deletedAt])
  @@map("promotional_offers")
}

model Gift {
  id                String     @id @default(cuid())
  name              String
  slug              String     @unique
  description       String?
  categoryId        String     @map("category_id")
  providerId        String     @map("provider_id")
  price             Decimal    @db.Decimal(10, 2)
  currency          String     @default("USD")
  imageUrls         Json       @default("[]") @map("image_urls")
  status            GiftStatus @default(INACTIVE)
  isFeatured        Boolean    @default(false) @map("is_featured")
  ratingPlaceholder Decimal    @default(0) @map("rating_placeholder") @db.Decimal(3, 2)
  createdAt         DateTime   @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime   @updatedAt @map("updated_at") @db.Timestamptz

  category          GiftCategory       @relation(fields: [categoryId], references: [id])
  provider          User               @relation("GiftProvider", fields: [providerId], references: [id])
  promotionalOffers PromotionalOffer[]
  variants          GiftVariant[]
  cartItems         CartItem[]
  orderItems        OrderItem[]

  @@index([categoryId])
  @@index([providerId])
  @@index([status])
  @@map("gifts")
}

model GiftVariant {
  id        String   @id @default(cuid())
  giftId    String   @map("gift_id")
  name      String
  price     Decimal  @db.Decimal(10, 2)
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz

  gift       Gift        @relation(fields: [giftId], references: [id])
  cartItems  CartItem[]
  orderItems OrderItem[]

  @@index([giftId])
  @@map("gift_variants")
}

model Broadcast {
  id          String            @id @default(cuid())
  title       String
  message     String
  audience    BroadcastAudience @default(ALL_USERS)
  status      BroadcastStatus   @default(SENT)
  createdBy   String            @map("created_by")
  updatedBy   String?           @map("updated_by")
  startedAt   DateTime?         @map("started_at") @db.Timestamptz
  completedAt DateTime?         @map("completed_at") @db.Timestamptz
  createdAt   DateTime          @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime          @updatedAt @map("updated_at") @db.Timestamptz

  creator User  @relation("BroadcastCreator", fields: [createdBy], references: [id])
  updater User? @relation("BroadcastUpdater", fields: [updatedBy], references: [id])

  notifications Notification[]

  @@index([status, createdAt])
  @@map("broadcasts")
}

model Notification {
  id            String                    @id @default(cuid())
  recipientId   String                    @map("recipient_id")
  recipientType NotificationRecipientType @map("recipient_type")
  broadcastId   String?                   @map("broadcast_id")
  title         String
  message       String
  isRead        Boolean                   @default(false) @map("is_read")
  readAt        DateTime?                 @map("read_at") @db.Timestamptz
  createdAt     DateTime                  @default(now()) @map("created_at") @db.Timestamptz

  recipient User       @relation(fields: [recipientId], references: [id])
  broadcast Broadcast? @relation(fields: [broadcastId], references: [id])

  @@index([recipientId, isRead, createdAt])
  @@index([broadcastId])
  @@map("notifications")
}

model DeviceToken {
  id         String         @id @default(cuid())
  userId     String         @map("user_id")
  token      String         @unique
  platform   DevicePlatform
  deviceId   String?        @map("device_id")
  lastSeenAt DateTime       @default(now()) @map("last_seen_at") @db.Timestamptz
  createdAt  DateTime       @default(now()) @map("created_at") @db.Timestamptz
  updatedAt  DateTime       @updatedAt @map("updated_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("device_tokens")
}

model Review {
  id                     String            @id @default(cuid())
  reviewCode             String            @unique @map("review_code")
  orderId                String            @map("order_id")
  providerId             String            @map("provider_id")
  userId                 String            @map("user_id")
  rating                 Int
  comment                String
  source                 ReviewSource      @default(IN_APP)
  externalProfileUrl     String?           @map("external_profile_url")
  status                 ReviewStatus      @default(PENDING)
  severity               ReviewSeverity    @default(LOW)
  flagReason             ReviewFlagReason? @map("flag_reason")
  reportCount            Int               @default(0) @map("report_count")
  flagReasonsJson        Json              @default("[]") @map("flag_reasons_json")
  autoModerated          Boolean           @default(false) @map("auto_moderated")
  moderationConfidence   Int?              @map("moderation_confidence")
  detectedCategoriesJson Json              @default("[]") @map("detected_categories_json")
  likesCount             Int               @default(0) @map("likes_count")
  createdAt              DateTime          @default(now()) @map("created_at") @db.Timestamptz
  updatedAt              DateTime          @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt              DateTime?         @map("deleted_at") @db.Timestamptz

  order    Order           @relation(fields: [orderId], references: [id])
  provider User            @relation("ReviewProvider", fields: [providerId], references: [id])
  customer User            @relation("ReviewCustomer", fields: [userId], references: [id])
  response ReviewResponse?

  @@index([status, createdAt])
  @@index([source, createdAt])
  @@index([reportCount])
  @@index([severity, status])
  @@index([providerId, createdAt])
  @@index([userId, createdAt])
  @@index([orderId])
  @@index([deletedAt])
  @@map("reviews")
}

model ReviewResponse {
  id         String    @id @default(cuid())
  reviewId   String    @unique @map("review_id")
  providerId String    @map("provider_id")
  body       String
  createdAt  DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt  DateTime  @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt  DateTime? @map("deleted_at") @db.Timestamptz

  review   Review @relation(fields: [reviewId], references: [id])
  provider User   @relation("ReviewResponseProvider", fields: [providerId], references: [id])

  @@index([providerId, createdAt])
  @@index([deletedAt])
  @@map("review_responses")
}

model ReviewPolicy {
  id                    String   @id @default(cuid())
  autoApprovalRulesJson Json     @default("{}") @map("auto_approval_rules_json")
  spamDetectionJson     Json     @default("{}") @map("spam_detection_json")
  abuseThresholdsJson   Json     @default("{}") @map("abuse_thresholds_json")
  visibilityRulesJson   Json     @default("{}") @map("visibility_rules_json")
  autoModerationJson    Json     @default("{}") @map("auto_moderation_json")
  updatedById           String?  @map("updated_by_id")
  createdAt             DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt             DateTime @updatedAt @map("updated_at") @db.Timestamptz

  updatedBy User? @relation("ReviewPolicyUpdater", fields: [updatedById], references: [id])

  @@index([updatedById])
  @@map("review_policies")
}

model Conversation {
  id         String   @id @default(cuid())
  orderId    String   @map("order_id")
  userId     String   @map("user_id")
  providerId String   @map("provider_id")
  createdAt  DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt  DateTime @updatedAt @map("updated_at") @db.Timestamptz

  order    Order     @relation(fields: [orderId], references: [id])
  user     User      @relation("UserConversation", fields: [userId], references: [id])
  provider User      @relation("ProviderConversation", fields: [providerId], references: [id])
  messages Message[]

  @@unique([orderId, userId, providerId])
  @@index([userId])
  @@index([providerId])
  @@map("conversations")
}

model Message {
  id             String     @id @default(cuid())
  conversationId String     @map("conversation_id")
  senderType     SenderType @map("sender_type")
  senderId       String     @map("sender_id")
  content        String?
  attachmentUrl  String?    @map("attachment_url")
  isRead         Boolean    @default(false) @map("is_read")
  createdAt      DateTime   @default(now()) @map("created_at") @db.Timestamptz

  conversation Conversation @relation(fields: [conversationId], references: [id])

  @@index([conversationId, createdAt])
  @@map("messages")
}

model ProviderReport {
  id               String               @id @default(cuid())
  reporterUserId   String               @map("reporter_user_id")
  providerId       String               @map("provider_id")
  orderId          String?              @map("order_id")
  reason           ProviderReportReason
  details          String
  evidenceUrlsJson Json                 @default("[]") @map("evidence_urls_json")
  status           ProviderReportStatus @default(SUBMITTED)
  adminNotes       String?              @map("admin_notes")
  reviewedById     String?              @map("reviewed_by_id")
  reviewedAt       DateTime?            @map("reviewed_at") @db.Timestamptz
  createdAt        DateTime             @default(now()) @map("created_at") @db.Timestamptz
  updatedAt        DateTime             @updatedAt @map("updated_at") @db.Timestamptz

  reporter User   @relation("ProviderReportReporter", fields: [reporterUserId], references: [id])
  provider User   @relation("ProviderReportProvider", fields: [providerId], references: [id])
  order    Order? @relation(fields: [orderId], references: [id])
  reviewer User?  @relation("ProviderReportReviewer", fields: [reviewedById], references: [id])

  @@index([reporterUserId, createdAt])
  @@index([providerId, status])
  @@index([orderId])
  @@map("provider_reports")
}

model UserSafetyReport {
  id               String                 @id @default(cuid())
  reportId         String                 @unique @map("report_id")
  reporterUserId   String                 @map("reporter_user_id")
  reportedUserId   String                 @map("reported_user_id")
  reason           UserSafetyReportReason
  details          String
  sourceType       UserSafetySourceType   @map("source_type")
  sourceId         String?                @map("source_id")
  evidenceUrlsJson Json                   @default("[]") @map("evidence_urls_json")
  status           UserSafetyReportStatus @default(SUBMITTED)
  adminComment     String?                @map("admin_comment")
  reviewedById     String?                @map("reviewed_by_id")
  reviewedAt       DateTime?              @map("reviewed_at") @db.Timestamptz
  createdAt        DateTime               @default(now()) @map("created_at") @db.Timestamptz
  updatedAt        DateTime               @updatedAt @map("updated_at") @db.Timestamptz

  reporter User  @relation("UserSafetyReporter", fields: [reporterUserId], references: [id])
  reported User  @relation("UserSafetyReported", fields: [reportedUserId], references: [id])
  reviewer User? @relation("UserSafetyReviewer", fields: [reviewedById], references: [id])

  @@index([reporterUserId, createdAt])
  @@index([reportedUserId, status])
  @@index([sourceType, sourceId])
  @@index([reason, status])
  @@map("user_safety_reports")
}

model UserBlock {
  id            String   @id @default(cuid())
  blockerUserId String   @map("blocker_user_id")
  blockedUserId String   @map("blocked_user_id")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz

  blocker User @relation("UserBlockBlocker", fields: [blockerUserId], references: [id])
  blocked User @relation("UserBlockBlocked", fields: [blockedUserId], references: [id])

  @@unique([blockerUserId, blockedUserId])
  @@index([blockedUserId])
  @@map("user_blocks")
}

model SocialPost {
  id            String               @id @default(cuid())
  userId        String               @map("user_id")
  content       String
  mediaUrlsJson Json                 @default("[]") @map("media_urls_json")
  visibility    SocialPostVisibility @default(PUBLIC)
  status        SocialPostStatus     @default(ACTIVE)
  createdAt     DateTime             @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime             @updatedAt @map("updated_at") @db.Timestamptz

  user     User           @relation("SocialPostAuthor", fields: [userId], references: [id])
  reports  SocialReport[]
  warnings UserWarning[]

  @@index([userId, createdAt])
  @@index([visibility, status])
  @@map("social_posts")
}

model SocialReport {
  id           String               @id @default(cuid())
  reportId     String               @unique @map("report_id")
  postId       String               @map("post_id")
  reportedById String               @map("reported_by_id")
  reason       SocialReportReason
  comment      String?
  severity     SocialReportSeverity @default(LOW)
  status       SocialReportStatus   @default(PENDING)
  createdAt    DateTime             @default(now()) @map("created_at") @db.Timestamptz
  updatedAt    DateTime             @updatedAt @map("updated_at") @db.Timestamptz

  post       SocialPost    @relation(fields: [postId], references: [id])
  reportedBy User          @relation("SocialReportReporter", fields: [reportedById], references: [id])
  warnings   UserWarning[]

  @@index([postId, status, createdAt])
  @@index([reportedById, createdAt])
  @@index([reason, status])
  @@index([severity, status])
  @@map("social_reports")
}

model SocialReportingRule {
  id                String                        @id @default(cuid())
  reportCategory    String                        @unique @map("report_category")
  label             String
  description       String?
  iconKey           String?                       @map("icon_key")
  autoFlagThreshold Int                           @default(10) @map("auto_flag_threshold")
  escalationRule    SocialReportingEscalationRule @default(MANUAL_REVIEW) @map("escalation_rule")
  isActive          Boolean                       @default(true) @map("is_active")
  createdAt         DateTime                      @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime                      @updatedAt @map("updated_at") @db.Timestamptz
  deletedAt         DateTime?                     @map("deleted_at") @db.Timestamptz

  @@index([isActive, deletedAt])
  @@index([autoFlagThreshold])
  @@map("social_reporting_rules")
}

model UserWarning {
  id             String             @id @default(cuid())
  userId         String             @map("user_id")
  socialReportId String             @map("social_report_id")
  postId         String             @map("post_id")
  reason         SocialReportReason
  message        String
  issuedById     String             @map("issued_by_id")
  createdAt      DateTime           @default(now()) @map("created_at") @db.Timestamptz

  user     User         @relation("WarnedUser", fields: [userId], references: [id])
  report   SocialReport @relation(fields: [socialReportId], references: [id])
  post     SocialPost   @relation(fields: [postId], references: [id])
  issuedBy User         @relation("WarningIssuer", fields: [issuedById], references: [id])

  @@index([userId, createdAt])
  @@index([socialReportId])
  @@map("user_warnings")
}

model Dispute {
  id                       String                   @id @default(uuid())
  userId                   String                   @map("user_id")
  providerId               String                   @map("provider_id")
  orderId                  String                   @map("order_id")
  reason                   String
  description              String                   @db.Text
  evidenceUrlsJson         Json                     @default("[]") @map("evidence_urls_json")
  status                   DisputeStatus            @default(PENDING)
  providerDecision         ProviderDisputeDecision? @map("provider_decision")
  providerResponse         String?                  @map("provider_response") @db.Text
  providerEvidenceUrlsJson Json                     @default("[]") @map("provider_evidence_urls_json")
  adminNote                String?                  @map("admin_note") @db.Text
  decisionReason           DisputeDecisionReason?   @map("decision_reason")
  createdAt                DateTime                 @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                DateTime                 @updatedAt @map("updated_at") @db.Timestamptz

  user     User  @relation("DisputeUser", fields: [userId], references: [id])
  provider User  @relation("DisputeProvider", fields: [providerId], references: [id])
  order    Order @relation(fields: [orderId], references: [id])

  @@index([status, createdAt])
  @@index([userId])
  @@index([providerId])
  @@index([orderId])
  @@map("disputes")
}

model SystemSettings {
  id                         String   @id @default(cuid())
  applicationName            String   @default("Gift App") @map("application_name")
  supportEmail               String   @default("support@giftapp.com") @map("support_email")
  platformLogoUrl            String?  @map("platform_logo_url")
  stripePublishableKey       String?  @map("stripe_publishable_key")
  stripeSecretKey            String?  @map("stripe_secret_key")
  stripeWebhookSecret        String?  @map("stripe_webhook_secret")
  firebaseServiceAccountJson Json?    @map("firebase_service_account_json")
  awsS3BucketName            String?  @map("aws_s3_bucket_name")
  awsRegion                  String?  @map("aws_region")
  awsAccessKey               String?  @map("aws_access_key")
  awsSecretKey               String?  @map("aws_secret_key")
  smtpHost                   String?  @map("smtp_host")
  smtpPort                   Int?     @map("smtp_port")
  smtpUsername               String?  @map("smtp_username")
  smtpPassword               String?  @map("smtp_password")
  senderEmail                String?  @map("sender_email")
  senderName                 String?  @map("sender_name")
  platformRatePercent        Decimal  @default(5) @map("platform_rate_percent") @db.Decimal(5, 2)
  minimumPayoutThreshold     Decimal  @default(100) @map("minimum_payout_threshold") @db.Decimal(10, 2)
  updatedById                String?  @map("updated_by_id")
  createdAt                  DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                  DateTime @updatedAt @map("updated_at") @db.Timestamptz

  updatedBy User? @relation("SystemSettingsUpdater", fields: [updatedById], references: [id])

  @@map("system_settings")
}

model SeasonalTheme {
  id        String   @id @default(cuid())
  name      String
  imageUrl  String   @map("image_url")
  startsAt  DateTime @map("starts_at") @db.Timestamptz
  endsAt    DateTime @map("ends_at") @db.Timestamptz
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz

  @@index([isActive, startsAt, endsAt])
  @@map("seasonal_themes")
}

model CommissionTier {
  id                    String    @id @default(cuid())
  name                  String
  commissionRatePercent Decimal   @map("commission_rate_percent") @db.Decimal(5, 2)
  orderVolumeThreshold  Decimal   @map("order_volume_threshold") @db.Decimal(10, 2)
  sortOrder             Int       @default(1) @map("sort_order")
  isActive              Boolean   @default(true) @map("is_active")
  updatedById           String?   @map("updated_by_id")
  deletedAt             DateTime? @map("deleted_at") @db.Timestamptz
  createdAt             DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt             DateTime  @updatedAt @map("updated_at") @db.Timestamptz

  updatedBy User? @relation("CommissionTierUpdater", fields: [updatedById], references: [id])

  @@index([deletedAt, isActive, sortOrder])
  @@index([orderVolumeThreshold, deletedAt])
  @@map("commission_tiers")
}

model ProviderPayoutMethod {
  id                 String                           @id @default(cuid())
  providerId         String                           @map("provider_id")
  type               ProviderPayoutMethodType         @default(BANK_ACCOUNT)
  accountHolderName  String                           @map("account_holder_name")
  bankName           String                           @map("bank_name")
  accountType        ProviderPayoutAccountType        @map("account_type")
  country            String
  currency           String                           @default("USD")
  maskedAccount      String                           @map("masked_account")
  last4              String
  payerId            String                           @map("payer_id")
  externalProvider   ProviderPayoutExternalProvider   @default(MANUAL) @map("external_provider")
  externalAccountId  String?                          @map("external_account_id")
  fingerprint        String?                          @map("fingerprint")
  verificationStatus ProviderPayoutVerificationStatus @default(PENDING) @map("verification_status")
  isDefault          Boolean                          @default(false) @map("is_default")
  isActive           Boolean                          @default(true) @map("is_active")
  deletedAt          DateTime?                        @map("deleted_at") @db.Timestamptz
  createdAt          DateTime                         @default(now()) @map("created_at") @db.Timestamptz
  updatedAt          DateTime                         @updatedAt @map("updated_at") @db.Timestamptz

  provider User @relation(fields: [providerId], references: [id])

  @@unique([providerId, fingerprint])
  @@index([providerId, deletedAt])
  @@index([providerId, isDefault])
  @@index([providerId, verificationStatus])
  @@map("provider_payout_methods")
}

model AuthSession {
  id               String    @id @default(cuid())
  userId           String    @map("user_id")
  refreshTokenHash String    @map("refresh_token_hash")
  deviceName       String?   @map("device_name")
  ipAddress        String?   @map("ip_address")
  location         String?
  userAgent        String?   @map("user_agent")
  lastActiveAt     DateTime  @default(now()) @map("last_active_at") @db.Timestamptz
  revokedAt        DateTime? @map("revoked_at") @db.Timestamptz
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt        DateTime  @updatedAt @map("updated_at") @db.Timestamptz

  user User @relation(fields: [userId], references: [id])

  @@index([userId, revokedAt])
  @@map("auth_sessions")
}

model SubscriptionPlan {
  id                           String                     @id @default(cuid())
  name                         String
  slug                         String                     @unique
  description                  String?
  monthlyPrice                 Decimal                    @default(0) @map("monthly_price") @db.Decimal(10, 2)
  yearlyPrice                  Decimal                    @default(0) @map("yearly_price") @db.Decimal(10, 2)
  currency                     String                     @default("USD")
  stripeProductId              String?                    @map("stripe_product_id")
  stripeMonthlyPriceId         String?                    @map("stripe_monthly_price_id")
  stripeYearlyPriceId          String?                    @map("stripe_yearly_price_id")
  status                       SubscriptionPlanStatus     @default(ACTIVE)
  visibility                   SubscriptionPlanVisibility @default(PUBLIC)
  isPopular                    Boolean                    @default(false) @map("is_popular")
  featuresJson                 Json                       @default("{}") @map("features_json")
  limitsJson                   Json                       @default("{}") @map("limits_json")
  activeSubscribersPlaceholder Int                        @default(0) @map("active_subscribers_placeholder")
  createdAt                    DateTime                   @default(now()) @map("created_at") @db.Timestamptz
  updatedAt                    DateTime                   @updatedAt @map("updated_at") @db.Timestamptz

  customerSubscriptions CustomerSubscription[]

  @@index([status, visibility])
  @@map("subscription_plans")
}

model PlanFeatureCatalog {
  id          String    @id @default(cuid())
  key         String    @unique
  label       String
  description String?
  type        String    @default("BOOLEAN")
  isActive    Boolean   @default(true) @map("is_active")
  sortOrder   Int       @default(0) @map("sort_order")
  deletedAt   DateTime? @map("deleted_at") @db.Timestamptz
  createdAt   DateTime  @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime  @updatedAt @map("updated_at") @db.Timestamptz

  @@index([isActive, sortOrder])
  @@index([deletedAt])
  @@map("plan_feature_catalog")
}

model UploadedFile {
  id              String             @id @default(cuid())
  ownerId         String             @map("owner_id")
  ownerRole       UserRole           @map("owner_role")
  targetAccountId String?            @map("target_account_id")
  folder          String
  fileName        String             @map("file_name")
  contentType     String             @map("content_type")
  sizeBytes       Int?               @map("size_bytes")
  fileUrl         String             @map("file_url")
  storageKey      String             @unique @map("storage_key")
  status          UploadedFileStatus @default(PENDING)
  giftId          String?            @map("gift_id")
  createdAt       DateTime           @default(now()) @map("created_at") @db.Timestamptz
  updatedAt       DateTime           @updatedAt @map("updated_at") @db.Timestamptz
  completedAt     DateTime?          @map("completed_at") @db.Timestamptz
  deletedAt       DateTime?          @map("deleted_at") @db.Timestamptz

  owner User @relation(fields: [ownerId], references: [id])

  @@index([ownerId, status])
  @@index([targetAccountId])
  @@index([giftId])
  @@index([folder, createdAt])
  @@index([deletedAt])
  @@map("uploaded_files")
}

model StaffRole {
  id          String   @id @default(cuid())
  name        String
  slug        String   @unique
  description String?
  permissions Json     @map("permissions_json")
  createdAt   DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt   DateTime @updatedAt @map("updated_at") @db.Timestamptz

  staffProfiles StaffProfile[]

  @@map("staff_roles")
}

model AdminAuditLog {
  id            String           @id @default(cuid())
  logReference  String?          @unique @map("log_reference")
  actorId       String?          @map("actor_id")
  actorType     String?          @map("actor_type")
  actorSnapshot Json?            @map("actor_snapshot")
  targetId      String?          @map("target_id")
  targetType    String?          @map("target_type")
  action        String
  actionLabel   String?          @map("action_label")
  module        String?
  status        AuditLogStatus   @default(SUCCESS)
  severity      AuditLogSeverity @default(LOW)
  beforeJson    Json?            @map("before_json")
  afterJson     Json?            @map("after_json")
  ipAddress     String?          @map("ip_address")
  createdAt     DateTime         @default(now()) @map("created_at") @db.Timestamptz

  actor User? @relation("AuditActor", fields: [actorId], references: [id])

  @@index([actorId, createdAt])
  @@index([targetId, createdAt])
  @@index([targetType, createdAt])
  @@index([action, createdAt])
  @@index([status, createdAt])
  @@index([module, createdAt])
  @@map("admin_audit_logs")
}

enum ProviderDocumentStatus {
  PENDING
  APPROVED
  REJECTED
}

model Document {
  id         String   @id @default(cuid())
  name       String   @unique
  isRequired Boolean  @default(false) @map("is_required")
  isActive   Boolean  @default(true) @map("is_active")
  createdAt  DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt  DateTime @updatedAt @map("updated_at") @db.Timestamptz

  providerDocuments ProviderDocument[]

  @@map("document_types")
}

model ProviderDocument {
  id                String                 @id @default(cuid())
  providerProfileId String                 @map("provider_profile_id")
  documentTypeId    String                 @map("document_type_id")
  fileUrl           String                 @map("file_url")
  status            ProviderDocumentStatus @default(PENDING)
  createdAt         DateTime               @default(now()) @map("created_at") @db.Timestamptz
  updatedAt         DateTime               @updatedAt @map("updated_at") @db.Timestamptz

  providerProfile ProviderProfile @relation(fields: [providerProfileId], references: [id], onDelete: Cascade)
  documentType    Document        @relation(fields: [documentTypeId], references: [id])

  @@unique([providerProfileId, documentTypeId])
  @@index([providerProfileId])
  @@index([documentTypeId])
  @@map("provider_documents")
}
