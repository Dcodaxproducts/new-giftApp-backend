import { Injectable } from '@nestjs/common';
import { Prisma, UserRole } from '@prisma/client';
import { PrismaService } from '../../../database/prisma.service';
@Injectable()
export class BlockedUsersRepository { constructor(private readonly prisma: PrismaService) {} findUserById(id: string) { return this.prisma.user.findFirst({ where: { id, role: { in: [UserRole.REGISTERED_USER, UserRole.PROVIDER] }, deletedAt: null }, select: { id: true, firstName: true, lastName: true, avatarUrl: true, role: true } }); } upsertBlock(blockerUserId: string, blockedUserId: string) { return this.prisma.userBlock.upsert({ where: { blockerUserId_blockedUserId: { blockerUserId, blockedUserId } }, create: { blockerUserId, blockedUserId }, update: {}, include: { blocked: { select: { id: true, firstName: true, lastName: true, avatarUrl: true, role: true } } } }); } deleteBlock(blockerUserId: string, blockedUserId: string) { return this.prisma.userBlock.deleteMany({ where: { blockerUserId, blockedUserId } }); } findBlocksAndCount(params: { where: Prisma.UserBlockWhereInput; skip: number; take: number }) { return this.prisma.$transaction([this.prisma.userBlock.findMany({ where: params.where, include: { blocked: { select: { id: true, firstName: true, lastName: true, avatarUrl: true, role: true } } }, orderBy: { createdAt: 'desc' }, skip: params.skip, take: params.take }), this.prisma.userBlock.count({ where: params.where })]); } findBlockBetween(firstUserId: string, secondUserId: string) { return this.prisma.userBlock.findFirst({ where: { OR: [{ blockerUserId: firstUserId, blockedUserId: secondUserId }, { blockerUserId: secondUserId, blockedUserId: firstUserId }] } }); } }
